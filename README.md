# Proyecto final de Desarrollo de Software

* En este proyecto esta anexado el front aparte y el backend aparte, adicional se tiene en el apartado de Actions unos test y pruebas que se ejecutan al iniciar o subir en un repositorio automaticamente.

* El front esta en la carpeta Nova-master.

* El backend es todo el contenido que esta como tal en el repositorio.

-----

Para iniciar el front es aconsejable entrar a la carpeta  del entorno es decir:

``` 
cd/NovaApp/Nova-master
``` 

una vez dentro del entorno ponemos el siguiente comando
``` 
npm install expo
``` 
ya instalado el expo go procedemos a poner el siguiente comando
``` 
npx expo start -c
``` 
esto nos generara dos cosas para entrar, entonces en nuestro dispositivo descargamos Expo Go y escaneamos el código que nos aparece en la consola o ponemos el url que sale abajo del código que se genero.


Metro waiting on exp://ip:puerto



## Integrantes:Julian Andres Pedraza, Joel Felipe Alba , Camilo Andres Otalora
